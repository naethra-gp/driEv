import 'package:flutter/material.dart';

class BikeCardWithHelp extends StatelessWidget {
  const BikeCardWithHelp({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
